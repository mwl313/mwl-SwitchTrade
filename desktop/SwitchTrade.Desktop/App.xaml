<Application x:Class="SwitchTrade.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="Ink" Color="#17312A" />
    <SolidColorBrush x:Key="Paper" Color="#F7F2E8" />
    <SolidColorBrush x:Key="Emerald" Color="#46B78A" />
    <SolidColorBrush x:Key="EmeraldDark" Color="#1E7157" />
    <SolidColorBrush x:Key="Blue" Color="#296C91" />
    <SolidColorBrush x:Key="Danger" Color="#A6463D" />
    <Style TargetType="Button">
      <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
      <Setter Property="FontSize" Value="16" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource Ink}" />
      <Setter Property="Background" Value="#FFFDF7" />
      <Setter Property="BorderBrush" Value="#17312A" />
      <Setter Property="BorderThickness" Value="2" />
      <Setter Property="Padding" Value="18,12" />
      <Setter Property="Margin" Value="0,0,0,12" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="Cursor" Value="Hand" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#D9F3E7" />
        </Trigger>
        <Trigger Property="IsKeyboardFocused" Value="True">
          <Setter Property="BorderBrush" Value="#296C91" />
          <Setter Property="BorderThickness" Value="4" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
      <Setter Property="FontSize" Value="17" />
      <Setter Property="Foreground" Value="{StaticResource Ink}" />
      <Setter Property="Background" Value="#FFFDF7" />
      <Setter Property="BorderBrush" Value="#17312A" />
      <Setter Property="BorderThickness" Value="2" />
      <Setter Property="Padding" Value="12,10" />
      <Setter Property="Margin" Value="0,7,0,16" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
      <Setter Property="FontSize" Value="16" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="Margin" Value="0,7,0,16" />
    </Style>
  </Application.Resources>
</Application>
