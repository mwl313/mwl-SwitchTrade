# frlg-ldn-trade runtime dependencies (tested versions; install into a venv)
ldn==0.0.17
pycryptodome==3.23.0
trio==0.33.0
websockets==17.0.1
zstandard==0.25.0

# Unified-repository relay runtime (relay/server.py and test_relay_offline.py).
fastapi==0.116.1
uvicorn==0.35.0

# Transitive runtime lock. Keep this list synchronized with the clean WSL
# dependency audit so provisioning cannot silently change between builds.
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
click==8.4.2
h11==0.16.0
idna==3.19
outcome==1.3.0.post0
pydantic==2.13.4
pydantic-core==2.46.4
python-netlink==0.0.15
sniffio==1.3.1
sortedcontainers==2.4.0
starlette==0.47.3
typing-inspection==0.4.4
typing-extensions==4.16.0
