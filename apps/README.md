# Applications

- `desktop/` is the primary native Windows WPF client and publish script.
- `web/` is the optional local web/debug client and shared visual reference implementation.

Both clients use the same local JSON control API. Radio, protocol, and driver behavior remains in the
Python/WSL runtime at the repository root.
